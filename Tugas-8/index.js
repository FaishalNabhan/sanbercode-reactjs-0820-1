var readBooks = require('./callback.js')
 
var books = [
    {name: 'LOTR', timeSpent: 3000}, 
    {name: 'Fidas', timeSpent: 2000}, 
    {name: 'Kalkulus', timeSpent: 4000},
    {name: 'komik', timeSpent: 1000}
]

readBooks = (10000, books, function(mulaibaca) {
    if (mulaibaca) {
        console.log(books[i])
        for(i=0; i<=3; i++)
    }
})